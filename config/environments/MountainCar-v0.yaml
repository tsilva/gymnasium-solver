# MountainCar standard challenge configurations

env_id: MountainCar-v0
eval_freq_epochs: 10
eval_episodes: 10

ppo:
  algo_id: ppo
  n_envs: 16
  n_timesteps: 1e6
  n_epochs: 10
  batch_size: 128
  normalize: false
  n_steps: 128
  gamma: 0.999
  learning_rate: 3e-4
  hidden_dims: [256, 256]
  max_epochs: 1000

reinforce:
  algo_id: reinforce
  n_envs: 8
  n_steps: 1024
  batch_size: 128
  gamma: 0.999
  learning_rate: 1e-3
  hidden_dims: [128, 128]
  max_epochs: 2000
